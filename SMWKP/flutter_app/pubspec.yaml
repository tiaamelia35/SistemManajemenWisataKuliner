name: smwkp_app
description: Flutter app skeleton for SMWKP
publish_to: 'none'
version: 0.0.1

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.0
  provider: ^6.0.0
  flutter_secure_storage: ^8.0.0
  image_picker: ^0.8.7
  webview_flutter: ^4.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
